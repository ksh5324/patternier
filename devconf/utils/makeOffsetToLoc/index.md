# src/utils/makeOffsetToLoc/index.ts

## 개요
바이트 오프셋을 라인/컬럼으로 변환하기 위한 매핑을 생성합니다.

## 입력과 출력
- 입력: 원본 코드 문자열
- 출력: `offsetToLoc` 함수

## 동작 흐름
- 코드 전체를 순회해 바이트/코드유닛 오프셋 인덱스를 생성
- 라인 시작 오프셋 배열을 구축
- 바이트 오프셋을 코드유닛 오프셋으로 변환 후 `{line, col}` 계산

## 확장 포인트
- 대용량 파일을 위한 메모리 최적화
- 다른 인코딩 지원
